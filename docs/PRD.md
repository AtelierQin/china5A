## 1. 项目背景与目标 (Background & Objectives)

### 1.1 背景
目前，中国国家5A级旅游景区名单主要以文字列表形式存在（如维基百科），缺乏直观的地理位置展示。旅游爱好者在规划行程或回顾旅行经历时，难以宏观地看到景点分布，也缺乏一个专门针对5A级景区的“打卡”与进度管理工具。

### 1.2 产品目标
1.  **数据可视化**：将维基百科的5A景区数据清洗后，精准投射在地图上。
2.  **个人记录**：提供用户标记“去过/想去”的功能，记录旅行时间与心得。
3.  **数据统计**：可视化展示用户的“探索进度”（如：全国完成率、各省份点亮情况）。

### 1.3 核心价值
让“旅行”变成一场可视化的“探索游戏”，量化旅行成就。

---

## 2. 用户画像 (User Personas)

*   **核心用户（你自己）**：拥有明确的旅行目标，希望通过地图直观查看哪些省份的5A景区密集，方便规划下一次行程；同时希望记录已完成的景点。
*   **扩展用户（大众旅行者）**：寻找高质量旅游目的地的人群，希望避开低质量景点，只去5A级。

---

## 3. 功能需求 (Functional Requirements)

### 3.1 核心模块：全景地图 (The Map)
这是网站的主界面。

*   **地图引擎**：使用 **Leaflet** 搭配 Carto/OpenStreetMap 的浅色系底图。
*   **景点标记 (Markers)**：
    *   根据经纬度在地图上显示所有5A景区。
    *   **状态区分**：
        *   *灰色图标*：未去过 (Unvisited)。
        *   *深色/自定义图标*：已打卡 (Visited)。
*   **聚合功能**：(V2.0) 当缩放级别较小时（看全国视角），将密集的点聚合显示。目前为了信息高密度展示，全量平铺。
*   **搜索与筛选**：
    *   文本模糊搜索（支持按省份/景区名称）。
    *   按状态筛选（全部/只看未去/只看已去）。

### 3.2 列表与详情 (List & Details)
*   **侧边栏/列表模式**：除了地图，提供一个可搜索与筛选的列表。支持导出/导入用户的 JSON 数据集。
*   **详情弹窗 (Pop-up)**：点击地图标记或列表项，弹出详情卡片。
    *   基础信息：名称、封面图、简介（基于 JSON 加载）。
    *   **操作栏**：[打卡/取消打卡]。
    *   扩展信息（V2.0）：加入心愿单、导航（跳转外部地图）、当前天气、参考门票价格等。

### 3.3 用户记录系统 (Check-in System)
*   **打卡交互**：用户点击弹窗中的“打卡/取消打卡”按钮，直接切换当前景区的状态。
    *   (V2.0规划)：增加必填打卡日期、选填评价和上传照片。
*   **数据持久化**：
    *   *MVP版本（最小可行性产品）*：使用浏览器 LocalStorage 保存记录（避免用户登录负担），并提供专门的 Data Export/Import 按钮实现跨设备迁移。
    *   *正式版本*：账号登录系统（云端同步数据，换手机数据不丢）。

### 3.4 成就仪表盘 (Dashboard)
*   **动态进度条**：
    *   根据当前查看的类别（如：CHINA 5A, WORLD 50, DESTINATIONS 225）动态显示探索进度。
    *   例如 "已探索 25 / 359 个" 或 "已探索 10 / 235 个"。
*   **生成分享卡片**：一键生成一张精美的图片，包含地图截图和统计数据，用于发朋友圈（例如：“我已制霸所有5A景区 10%”）。

---

## 4. 数据需求 (Data Requirements)

这是项目最关键的部分，需要从维基百科转化为结构化数据。

1.  **数据源**：https://zh.wikipedia.org/wiki/国家5A级旅游景区名单
2.  **数据清洗流程**：
    *   **Step 1 (Scraping)**: 编写脚本（Python/Node.js）爬取维基百科表格。
    *   **Step 2 (Geocoding)**: 维基百科只有地名，没有经纬度。需要调用高德/百度地图的 **地理编码 API**，将“四川省成都市青城山-都江堰旅游景区”转换为 `Lat: 30.9, Lng: 103.6`。
    *   **Step 3 (Storage)**: 存入 JSON 文件或数据库（SQLite/MySQL）。
    *   *注意*：部分景区范围很大，取主门坐标即可。

---

## 5. 技术栈推荐 (Tech Stack)

为了快速开发并兼顾极简主义设计原则，当前使用以下极简前端架构：

*   **前端逻辑**：Vanilla JS (原生 JS 模块化)，无复杂的前端构建和响应式框架，轻量级加载。
*   **样式与UI**：Vanilla CSS 配合 CSS 变量体系，自行实现底层设计系统。
*   **地图组件**：`Leaflet` 与 Carto 浅色开源地图资源。
*   **后端**：纯静态前端项目。数据抓取预处理为 `attractions.json`，用户状态存在本地存储。
*   **部署**：Vercel 或 GitHub Pages 等静态服务托管平台快速上线。

---

## 6. 交互设计草图 (UI Wireframe Concept)

### 页面布局
```text
+-------------------------------------------------------+
|  LOGO [5A足迹]    我的进度: 12/330    [用户头像/登录]   |
+----------------------+--------------------------------+
|  搜索景区...       |                                |
|  [筛选: 四川 x]    |                                |
|                     |                                |
|  1. 九寨沟 [已去]  |                                |
|     2015年打卡     |          地图区域               |
|                     |                                |
|  2. 峨眉山 [想去]  |       (展示中国地图)           |
|                     |        📍      📍              |
|  3. 黄龙景区       |            📍                  |
|                     |       🚩(已去是旗帜)           |
|  ...                |                                |
|                     |                                |
+----------------------+--------------------------------+
```

---

## 7. 开发路线图 (Roadmap)

### 第一阶段：MVP (最小可行性产品) (已完成 ✅)
*   完成数据爬取和经纬度转换，生成 `attractions.json` (359 处)。
*   搭建前端页面，加载开源流图底图。
*   读取 JSON 数据，在地图上渲染所有 Markers，弹窗支持打卡交互。

### 第二阶段：交互与多维数据引擎 (已完成 ✅)
*   添加侧边栏列表与多维度数据筛选源 (NatGeo World 50, China 50, 225)。
*   实现“标记状态”功能（基于 LocalStorage），提供导入导出。
*   右上角监控面板根据当前选中的 Category 自动计算达成率。

### 第三阶段：统计与优化
*   移动端适配（手机浏览器打开体验良好）。
*   增加“一键导航”唤起手机地图App等本地化功能。

---

## 8. 给开发者的特别提示 (Tips)

1.  **坐标系问题**：这是地图开发最大的坑。
    *   维基百科/国际数据通常是 **WGS-84** 坐标。
    *   高德/腾讯地图使用 **GCJ-02** (火星坐标系)。
    *   百度地图使用 **BD-09**。
    *   **解决方案**：获取经纬度时直接使用你所选地图厂商的API（例如用高德API查坐标，在显示时也用高德地图），这样能保证位置准确吻合。
2.  **数据更新**：5A景区名单虽然稳定，但每年会有新增或摘牌（极少）。建议每半年手动或脚本校对一次维基百科。
3.  **景区合并**：维基百科上有些是一项包含多个点（如“苏州园林”包含拙政园、留园等）。在地图上，你可能需要将其拆分为多个坐标点，或者取一个中心点。
